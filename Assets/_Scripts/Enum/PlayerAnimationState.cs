namespace SpellFlinger.Enum
{
    public enum PlayerAnimationState
    {
        Idle = 1,
        RunForward = 2,
        RunBack = 3,
        Jumping = 4,
        Dead = 5,
        Attack = 6,
    }
}
